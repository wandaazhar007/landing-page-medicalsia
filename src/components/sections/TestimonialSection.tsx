// Testimonial placeholder — HIDDEN until Medicalsia has its first client
// (see docs/09-landing-page-brief.md, never fill this with fake testimonials).
const TESTIMONIALS_VISIBLE = false;

export default function TestimonialSection() {
  if (!TESTIMONIALS_VISIBLE) {
    return null;
  }

  return null;
}
